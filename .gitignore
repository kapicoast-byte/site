node_modules/
.next/
out/
build/
.env
.env.local
*.log
.DS_Store
next-env.d.ts
/uploads/*
!/uploads/.gitkeep
tsconfig.tsbuildinfo
.env*
!.env.example
.pgdata/

# Firebase service-account keys — never commit these. They bypass every
# security rule and work from anywhere.
service-account*.json
serviceAccount*.json
*-firebase-adminsdk-*.json

# Agent skills installed locally (npx skills add). Reference material for
# editors, not part of the site — it has no business in the image or the repo.
.agents/

# Generated dish photography and its cached originals — 180 MB, and the
# images live in Firebase Storage once attached. Regenerate with
# scripts/gen-dish-images.mjs; the manifest records every prompt.
public/dishes/
# Receipt for scripts/attach-dish-images.mjs --rollback. Local, not shared.
scripts/.attach-receipt.json
